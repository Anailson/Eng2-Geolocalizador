@model IEnumerable<Dominio.Classes.Equipe>
<nav>
    <span class="card-title">Lista de áreas cadastradas</span>
</nav>

<div class="row">
    <br /><br />
    @Html.ActionLink("Inicio", "Index", "Home")
</div>
<div class="row">
    <form class="col s12">
        <p>
            <div class="row">
                <!-- Radio Button -->
                <div class="input-field col s2">
                    <input name="pesquisa-equipe" class="with-gap" type="radio" id="equipe" checked />
                    <label for="equipe">N° da equipe</label>
                </div>
                <!-- Caixa de texto -->
                <div class="input-field col s3">
                    <i class="material-icons prefix">search</i>
                    <input id="pesquisa" type="text" placeholder="Pesquisar" />
                </div>
                <!-- Botão Novo -->
                <div class="input-field col s3">
                    <label for="pesquisa">@Html.ActionLink("Adicionar Novo", "Inserir")</label>
                </div>
            </div>
        </p>
    </form>
</div>

<table class="striped">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.codigo)
            </th>
            <th></th>
        </tr>
    </thead>
    @foreach (var item in Model)
    {
        <tbody>
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.codigo)
                </td>

                <td>
                    @Html.ActionLink("Editar", "Editar", new { id = item.id }) |
                    @Html.ActionLink("Detalhes", "Detalhes", new { id = item.id }) |
                    @Html.ActionLink("Remover", "Remover", new { id = item.id })
                </td>
            </tr>
        </tbody>
    }
</table>