@model IEnumerable<Dominio.Classes.Equipe>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Adiconar Novo", "Inserir")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.codigo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.numSus)
        </th>
        
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.codigo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.numSus)
        </td>
        
        <td>
            @Html.ActionLink("Editar", "Editar", new { id=item.numSus }) |
            @Html.ActionLink("Detalhes", "Detalhes", new { id = item.numSus }) |
            @Html.ActionLink("Remover", "Remover", new { id = item.numSus })
        </td>
    </tr>
}

</table>
