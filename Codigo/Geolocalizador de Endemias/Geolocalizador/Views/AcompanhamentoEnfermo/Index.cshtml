@model IEnumerable<Dominio.Classes.AcompanhamentoEnfermo>
<nav>
    <span class="card-title">Lista de acompanhamentos de efermos</span>
</nav>
<div class="row">
    <br /><br />
    @Html.ActionLink("Página Inicial", "Index", "Home") >
</div>
<div class="row">
    <form class="col s12">
        <p>
            <div class="row">
                <!-- Radio Button -->
                <div class="input-field col s2">
                    <input name="pesquisa-acomp-enfermo" class="with-gap" type="radio" id="acomp-enfermo-sus" checked />
                    <label for="acomp-enfermo-sus">N° do sus do enfermo</label>
                </div>
                <!-- Caixa de texto -->
                <div class="input-field col s3">
                    <i class="material-icons prefix">search</i>
                    <input id="pesquisa" type="text" placeholder="Pesquisar" />
                </div>
                <!-- Botão Novo -->
                <div class="input-field col s3">
                    @Html.ActionLink("Novo", "Inserir", null, new {@class="btn blue" })
                </div>
            </div>
        </p>
    </form>
</div>

<table class="table">
    <thead class="striped">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Visita.dataVisita)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.codVisita)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.numSus)
            </th>
            <th></th>
        </tr>
    </thead>

    @foreach (var item in Model)
    {
        <tbody>
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Visita.dataVisita)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.codVisita)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.numSus)
                </td>
                <td>
                    @Html.ActionLink("Editar", "Editar", new { id = item.id }, new { @class = "btn blue" })
                    @Html.ActionLink("Detalhes", "Detalhes", new { id = item.id }, new { @class = "btn blue" })
                    @Html.ActionLink("Remover", "Remover", new { id = item.id }, new { @class = "btn blue" })
                </td>
            </tr>
        </tbody>
    }

</table>
