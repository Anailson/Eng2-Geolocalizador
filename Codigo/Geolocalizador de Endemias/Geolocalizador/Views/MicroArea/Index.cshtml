@model IEnumerable<Dominio.Classes.MicroArea>
<nav>
    <span class="card-title">Lista de micro áreas cadastradas</span>
</nav>
<div class="row">
    <br /><br />
    @Html.ActionLink("Página Inicial", "Index", "Home")
</div>
<div class="row">
    <form class="col s12">
        <p>
            <div class="row">
                <!-- Radio Button -->
                <div class="input-field col s2">
                    <input name="pesquisa-mciro" class="with-gap" type="radio" id="micro" checked />
                    <label for="micro">N° da micro área</label>
                </div>
                <!-- Caixa de texto -->
                <div class="input-field col s3">
                    <i class="material-icons prefix">search</i>
                    <input id="pesquisa" type="text" placeholder="Pesquisar" />
                </div>
                <!-- Botão Novo -->
                <div class="input-field col s3">
                    @Html.ActionLink(" Novo", "Inserir", null, new {@class="btn blue" })
                </div>
            </div>
        </p>
    </form>
</div>


<table class="bordered striped">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.codigo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.codArea)
            </th>
            <th></th>
        </tr>
    </thead>
    @foreach (var item in Model)
    {
        <tbody>
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.codigo)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.codArea)
                </td>
                <td>
                    @Html.ActionLink("Editar", "Editar", new { id = item.id }, new { @class = "btn blue" })
                    @Html.ActionLink("Detalhes", "Detalhes", new { id = item.id }, new { @class = "btn blue" })
                    @Html.ActionLink("Remover", "Remover", new { id = item.id }, new { @class = "btn blue" })
                </td>

            </tr>
        </tbody>
    }
</table>